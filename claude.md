# 모기제로 웹앱 개발 규칙

## 프로젝트 개요

이 프로젝트는 날씨 데이터와 지역별 데이터를 활용하여 오늘의 모기지수를 알려주는 웹앱이다.

사용자는 현재 위치 또는 선택한 지역의 모기 위험도, 날씨 정보, 모기 발생 가능성이 높은 장소와 예방 방법을 확인할 수 있어야 한다.

## 작업 기본 규칙

* 모든 설명과 주석은 한국어로 작성한다.
* 초보자도 이해할 수 있도록 코드를 단순하고 명확하게 작성한다.
* 작업하기 전에 website-request.txt 파일을 먼저 읽는다.
* website-request.txt에 작성된 요구사항을 기준으로 개발한다.
* 사용자가 요청하지 않은 기존 기능을 임의로 삭제하지 않는다.
* 파일을 수정하기 전에 관련 파일의 기존 내용을 먼저 확인한다.
* HTML, CSS, JavaScript를 서로 다른 파일로 분리한다.
* 한 파일에 모든 코드를 몰아서 작성하지 않는다.
* 함수와 변수 이름은 역할을 쉽게 알 수 있도록 작성한다.
* 중요한 기능에는 이해하기 쉬운 한국어 주석을 작성한다.

## 사용 기술

* HTML5
* CSS3
* JavaScript
* Leaflet.js
* OpenStreetMap
* Chart.js
* 날씨 API

별도의 요청이 없으면 React, Vue, TypeScript, Node.js 같은 기술은 사용하지 않는다.

## 파일 구성

기본적으로 다음 파일을 사용한다.

* index.html: 웹페이지 구조
* style.css: 웹페이지 디자인
* script.js: 모기지수 계산, 지도, 날씨 API, 화면 기능
* website-request.txt: 전체 개발 요구사항
* data/mosquito-sample.json: 모기 샘플 데이터
* data/regions.json: 지역 위치 데이터
* README.md: 설치 및 실행 방법

필요한 경우 파일을 추가할 수 있지만, 추가한 이유를 작업 완료 후 설명한다.

## 디자인 규칙

* 깔끔하고 현대적인 환경정보 앱 스타일로 제작한다.
* 날씨 앱이나 미세먼지 정보 앱처럼 정보를 한눈에 확인할 수 있도록 한다.
* 데스크톱, 태블릿, 모바일에서 모두 정상적으로 표시되도록 한다.
* 모바일 화면에서 가로 스크롤이 생기지 않도록 한다.
* 버튼은 모바일에서도 쉽게 누를 수 있는 크기로 제작한다.
* 위험 단계는 색상뿐 아니라 글자와 아이콘으로도 구분한다.
* 모기지수 숫자는 화면에서 가장 눈에 잘 띄게 표시한다.
* 카드형 디자인과 충분한 여백을 사용한다.
* 과도한 애니메이션은 사용하지 않는다.

## 모기지수 규칙

모기지수는 0점부터 100점까지 표시한다.

* 0~20점: 매우 양호
* 21~40점: 양호
* 41~60점: 보통
* 61~80점: 위험
* 81~100점: 매우 위험

각 단계에 맞는 색상, 안내 문구, 행동 요령을 함께 표시한다.

모기지수는 다음 요소를 참고하여 계산한다.

* 기온
* 습도
* 최근 강수량
* 현재 강수 여부
* 풍속
* 시간대
* 계절
* 지역별 모기 데이터

계산된 값은 참고용 예상값임을 화면에 명확하게 표시한다.

## 데이터 사용 규칙

* 실제 데이터와 샘플 데이터를 명확히 구분한다.
* 확인되지 않은 데이터를 실제 관측값이라고 표시하지 않는다.
* 존재하지 않는 API 주소를 임의로 만들지 않는다.
* API 응답 형식을 추측해서 구현하지 않는다.
* 외부 데이터를 사용하기 전에 공개 API 여부와 이용 조건을 확인한다.
* SODAMAP 데이터 연동이 가능한지 먼저 확인한다.
* SODAMAP 연동이 불가능하면 샘플 데이터를 사용한다.
* 데이터 연동이 불가능한 이유를 README.md에 기록한다.
* 외부 사이트의 보안이나 접근 제한을 비정상적으로 우회하지 않는다.
* API 키는 코드에 직접 공개하지 않는다.
* API 키는 config.js 등의 별도 파일로 관리한다.
* 실제 API 키가 포함된 파일은 GitHub에 올리지 않도록 안내한다.

## 오류 처리 규칙

* API 호출 실패 시 페이지 전체가 멈추지 않도록 한다.
* 날씨 데이터를 가져오지 못하면 샘플 데이터를 표시한다.
* 모기 데이터를 가져오지 못하면 날씨 기반 예상값을 표시한다.
* 위치정보 사용을 거부해도 지역 검색을 이용할 수 있도록 한다.
* 지도 로딩 실패 시 사용자에게 오류 안내를 표시한다.
* 오류 내용을 console에만 표시하지 말고 화면에도 쉽게 안내한다.
* 가능하면 다시 시도 버튼을 제공한다.
* 브라우저 콘솔에 치명적인 오류가 없도록 확인한다.

## 지도 규칙

* 기본 지도는 Leaflet.js와 OpenStreetMap을 사용한다.
* 현재 위치를 지도에 표시한다.
* 지역별 모기지수를 마커 또는 원형 마커로 표시한다.
* 마커를 누르면 지역명, 모기지수, 위험 단계, 날씨 정보가 나타나도록 한다.
* 위험 단계별 지도 범례를 표시한다.
* 실제 관측 데이터, 예상 데이터, 샘플 데이터를 구분한다.

## 접근성 규칙

* 이미지에는 alt 속성을 작성한다.
* 입력창에는 label을 연결한다.
* 키보드로 주요 버튼을 사용할 수 있도록 한다.
* 작은 글자는 최소 14px 이상으로 작성한다.
* 일반 본문은 약 16px로 작성한다.
* 글자와 배경의 명도 차이를 충분히 확보한다.
* 버튼 포커스 상태를 명확하게 표시한다.

## 작업 순서

다음 순서로 개발한다.

1. website-request.txt 확인
2. 프로젝트 파일 구조 확인
3. 기본 HTML 화면 작성
4. CSS 반응형 디자인 작성
5. 샘플 모기 데이터 연결
6. Leaflet 지도 구현
7. 모기지수 계산 기능 구현
8. 날씨 API 연결
9. 지역 검색 및 현재 위치 기능 구현
10. 로딩 및 오류 처리
11. SODAMAP 데이터 연동 가능 여부 확인
12. 브라우저 실행 테스트
13. README.md 작성

## 작업 완료 후 보고

작업이 끝나면 다음 내용을 한국어로 설명한다.

* 생성한 파일
* 수정한 파일
* 각 파일의 역할
* 웹사이트 실행 방법
* API 키 설정 방법
* 실제 데이터로 연결된 기능
* 샘플 데이터를 사용하는 기능
* 아직 구현하지 못한 기능
* 발생할 수 있는 오류
* 다음 단계에서 개선할 내용

작업 완료 후 index.html이 정상적으로 실행되는지 확인한다.
