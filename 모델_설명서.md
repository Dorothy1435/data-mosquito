# 김해시 모기 발생 위험도 모델 — 설명서 (v3)

## 한 줄 요약
> 김해시의 **발생원 시설 · 유충조사 · 인구** 데이터를 읍·면·동별로 합쳐서,
> **"어디가 모기 위험이 높고, 어디부터 방제해야 하는지"**를 점수로 알려주는 모델입니다.
> 여기에 **실시간 날씨**를 곱해 "오늘 이 동네 모기지수는 몇 점"까지 계산합니다.
> **v3의 핵심**은 **인구(노출도)** 를 넣어 *"많이 생기고 + 많이 물리는 곳"* 이 진짜 위험하도록 바꾼 것입니다.

---

## 1. 핵심 아이디어 — 발생 잠재력 × 인구 노출

모기는 **고인 물 + 유기물**에서 번식합니다. 그래서 **발생원 시설**(정화조·축사·저수지·폐타이어 등)이
많은 곳이 위험합니다. 하지만 발생원만 세면 **함정**이 있습니다 — 시설이 아무리 많아도
**물릴 사람이 없으면** 실제 피해는 없습니다.

```
발생 잠재력 (B)            ×            인구 노출 (E)          =     실제 위험
"얼마나 생기나"                    "얼마나 물리나"
↑ 발생원 시설 + 유충검출률          ↑ 인구가 많을수록 큼
(농촌이 큼: 가구별 정화조 수천)     (도심이 큼: 인구 밀집)
```

- 두 요소를 **곱(기하평균)** 으로 결합 → **둘 다 높아야** 위험이 커집니다.
- 그래서 **발생원만 많고 인구 적은 농촌**(예: 한림면)의 과대평가가 자동으로 교정됩니다.

여기에 **날씨**를 곱해 "오늘" 점수를 냅니다.

```
지역위험 = √( 발생잠재력 B × 인구노출 E )
모기지수 = 100 × 날씨활동지수 × (0.35 + 0.65 × 지역위험)
```

---

## 2. 두 개의 산출물

| | **방제 우선순위** (정적) | **모기지수** (동적) |
|---|---|---|
| 무엇 | 구역별 *연간* 방제 우선순위 | *오늘* 그 동네 모기 활동 점수 |
| 만든 곳 | 발생원 집계 파이프라인 | `mosquito_model_portable.py` (파이썬) / `mosquito-model.js` (웹) |
| 입력 | 시설·유충·인구 데이터 | 위 결과 + 날씨 |
| 용도 | 예산·인력 배분 (행정) | 시민용 일일 예보 (앱/웹) |
| 변하나? | 데이터 갱신 시에만 | 날씨 따라 매일 |

> **참조 구현은 `mosquito_model_portable.py`**(파이썬, 검증·백엔드용)이고,
> **실제 배포되는 웹앱은 이를 그대로 포팅한 `mosquito-model.js`**(브라우저용)입니다.
> 두 파일은 **계산식·가중치·데이터·인구결합(v3)이 동일**하도록 맞춰져 있습니다(9번 참고).

---

## 3. 사용한 데이터

| 데이터 | 역할 | 비고 |
|---|---|---|
| 개인하수처리시설(정화조·오수) | 발생원 (최대 유충서식처) | 9,579건 |
| 양돈·한우 농가 | 발생원 (분뇨·물웅덩이) | 761건 |
| 저수지 현황 | 발생원 (정체수) | 110건 |
| 타이어가게 | 발생원 (폐타이어 산란처) | 99건 |
| 폐기물 재활용/처리업체 | 발생원 (고인물·용기) | 769건 |
| 수경시설(녹지과·물놀이형) | 발생원 (정체수) | 33건 |
| 공중화장실 | 발생원 + 도심밀집 지표 | 563건 |
| 유충 실태조사 | 발생 잠재력 실측 보정 | 1,000+건 |
| **주민등록 인구(2026.6, 외국인 제외)** | **인구 노출도 (v3 신규)** | 17개 구역 |
| 방역소독 민원대장(2025) | **검증 타겟** (모델 입력엔 미사용) | 196건 |

> 인구는 모델 구역에 맞춰 통합: **활천동 = 활천동+삼안동**(행정동), **장유 = 장유1·2·3동** 합산.
> `.hwp/.pdf` 문서(하천·하수처리 등)는 자동 파싱이 불안정해 제외. 표(엑셀/CSV)로 주시면 추가 가능.

---

## 4. 계산 과정

1. **주소 → 행정구역 추출** — 모든 데이터의 주소에서 읍·면·동 자동 추출
2. **구역별 집계** — 시설 유형별 개수를 17개 구역에 집계 → `features_by_district.csv`
3. **발생 잠재력 B (0~1)** — 발생원 위험지수(시설 가중합)에 **실제 유충 검출률**을 결합
   - 발생원 가중치: 정화조 3.0 · 저수지 2.5 · 축산 2.0 · 타이어 2.0 · 폐기물재활용 1.5 · 수경시설 1.5 · 화장실 0.5
   - 유충 표본이 많을수록 실측 비중↑ (최대 60%)
   - ※ **민원은 B에 넣지 않습니다** (민원은 검증 타겟이라 넣으면 순환)
4. **인구 노출 E (0.15~1.0)** — 인구를 로그 정규화. 인구 많을수록 물릴 사람 많음
5. **지역위험 = √(B × E)** — 곱 결합으로 "생김 + 물림" 동시 반영
6. **모기지수 = 100 × 날씨활동지수 × (0.35 + 0.65 × 지역위험)**
   - 날씨활동지수: 기온(27°C 최적)·습도(높을수록↑)·강수(적당한 비↑, 폭우↓)를 0~1로
   - `0.35`는 날씨 좋으면 발생원 적어도 최소한의 모기는 있다는 기본값

---

## 5. 검증 — 인구를 넣으니 실제 민원과 맞아떨어짐

민원은 **검증 타겟으로만** 쓰고 지수 계산엔 넣지 않았습니다(순환 방지).
데이터 완전한 12개 구역 기준, 예측값과 실제 민원의 상관(스피어만):

| 예측값 | 실제 민원과 상관 |
|---|---|
| 발생원 시설만 | +0.55 |
| 발생원 + 유충 (= 발생잠재력 B) | +0.58 |
| 인구 노출만 (E) | +0.48 |
| **발생원 × 인구 (v3 지역위험)** | **+0.81** |

- 다중회귀 설명력(R²)도 발생원만 0.445 → 인구 추가 **0.664** (+0.22).
- 즉 **인구는 발생원 위에 진짜 독립적인 신호를 더합니다.** 이것이 v3의 핵심 근거입니다.

---

## 6. 핵심 순위 (7월 평년 기준, v3)

| 순위 | 구역 | 발생잠재력 | 인구 | 민원 | **모기지수** | 비고 |
|---|---|---|---|---|---|---|
| 1 | 진영읍 | 0.56 | 52,201 | 0 | 69.3 | ★결측·발생원多+인구多 |
| 2 | 주촌면 | 0.62 | 21,646 | 23 | 66.0 | |
| 3 | 활천동 | 0.40 | 70,234 | 37 | 64.7 | 도심·유지 |
| 4 | 한림면 | 1.00 | 6,310 | 0 | 64.2 | ★결측·**거품 제거**(구 1위) |
| 5 | 북부동 | 0.37 | 80,471 | 23 | 64.2 | |
| 6 | 장유 | 0.24 | 179,538 | 0 | 60.4 | ★결측·**대인구 재평가**(구 13위) |
| 7 | 내외동 | 0.20 | 66,323 | 15 | 54.8 | 인구↑ 상향 |
| 8 | 대동면 | 0.51 | 4,848 | 18 | 52.8 | 저인구 하향 |
| 9 | 진례면 | 0.46 | 5,370 | 0 | 52.6 | ★결측 |
| 10 | 생림면 | 0.70 | 3,294 | 9 | 52.2 | 저인구 하향(구 3위) |
| 11 | 상동면 | 0.59 | 2,852 | 21 | 48.9 | 저인구 하향(구 4위) |
| 12 | 부원동 | 0.19 | 8,722 | 23 | 47.1 | |
| 13 | 회현동 | 0.09 | 7,917 | 9 | 42.1 | |
| 14 | 동상동 | 0.07 | 8,231 | 6 | 40.6 | |
| 15 | 칠산서부동 | 0.06 | 8,427 | 3 | 40.0 | |
| 16 | 불암동 | 0.04 | 6,393 | 6 | 37.9 | |
| 17 | 화목동 | 0.07 | 3,000 | 3 | 37.8 | 인구 추정값 |

**교정 효과**: 발생원 시설만 많던 저인구 농촌(한림·생림·상동)이 내려가고,
인구 밀집 도심(활천·내외)과 대인구 구역(장유)이 실제 피해 규모에 맞게 재배치됐습니다.

---

## 7. 서부 결측 처리 (보건소 자료 미제공)

보건소 현장 유충조사·민원이 **없는 구역(한림·진영·진례·장유)** 은 특별 처리합니다.

- 이들의 **민원 0 / 조사 0** 은 "안전"이 아니라 **"결측"** 으로 취급 → 위험은 **인구·발생원으로 추정**
- 출력에 `data_gap: true`, **신뢰도 "낮음"**, 경고문 자동 표기
- 방제당국 조언에 **"현장 유충조사 우선 실시"** 자동 추가

> 즉 서부는 "위험할 수 있으니 데이터부터 확보"로 안내됩니다. **0건을 안전으로 오판하지 않습니다.**

---

## 8. 실시간 날씨 · 불확실성 출력

### 실시간 기상 연동 — Open-Meteo (구역별 날씨)
`live_weather=True`로 호출하면 **현재 날씨를 자동으로 받아와** 계산합니다.
- API 키 불필요, 표준 라이브러리(urllib)만 사용 → **파일 하나로 이식 가능** 유지
- 구역별 좌표(`COORDS`)로 17개 읍·면·동이 **각자 다른 날씨**로 계산됨
- `all_indices(live_weather=True)`는 **1회 호출**로 17곳을 한 번에 받아옴
- 인터넷 실패 시 **월평년값으로 자동 대체** (서비스 중단 없음)

### 불확실성 — 신뢰구간 + 신뢰도 등급
단일 점수의 *거짓 정밀도*를 없애고 **범위와 신뢰도**를 함께 반환합니다.
- 날씨 실측 + 유충 표본 많음 → **신뢰도 높음**, 좁은 구간
- 평년값 + 조사 없음(서부 결측) 또는 인구 추정(화목동) → **신뢰도 낮음**, 넓은 구간

### '지금 이 순간' 추가 보정 — 시간대·풍속·현재강수·발육(GDD)
위 v3 수식에 아래 보정을 곱해 더 정밀하게 계산합니다. **파이썬·JS 두 파일 모두 동일하게 구현**돼 있습니다.
- **시간대**: 일몰 직후(19~22시)·새벽(04~06시) 피크, 한낮·심야 저조
- **풍속**: 강풍(6m/s+)이면 모기가 못 날아 활동 하향
- **현재 강수**: 지금 비 오는 중이면 흡혈 활동 일시 감소
- **발육(GDD)**: 최근 ~2주 누적온도로 '성충이 나올 만큼 따뜻했나'를 반영(봄·가을 과대평가 억제)

> 이 보정값들은 **입력이 없으면 1.0**(보정 없음)이라, 날씨만 주면 파이썬·JS 순위가 완전히 일치합니다.
> (검증: 17개 구역 × 6가지 날씨 조합 = 102케이스에서 두 파일 결과 **불일치 0**.)

---

## 9. 배포 — 파일 하나만 끌고가면 됩니다

이 프로젝트는 **정적 웹앱(HTML/CSS/JS, Vercel 배포)** 이라 브라우저에서 파이썬이 돌지 않습니다.
그래서 **실제 웹앱은 [mosquito-model.js](mosquito-model.js) 한 파일**을 불러 `window.GimhaeMosquitoModel`로 씁니다.
이 JS는 [mosquito_model_portable.py](mosquito_model_portable.py)를 **계산식·가중치·데이터·인구결합(v3)까지 동일**하게 포팅한 것으로, 두 파일의 순위가 완전히 일치합니다.

- **외부 의존성 0** — JS/파이썬 모두 표준 기능만 사용 (파이썬 실시간 날씨만 `urllib`, 이것도 표준)
- 17개 구역의 발생원·유충·**인구** 데이터 + 수식이 **파일 안에 전부 내장**
- pandas·scikit-learn·pkl 파일 **불필요**

```javascript
// 웹(브라우저) — mosquito-model.js 하나만 <script>로 불러오면 됨
const r = GimhaeMosquitoModel.mosquitoIndex('활천동', { temp_c: 29, humidity: 80, rain_3d_mm: 50 });
const ranked = GimhaeMosquitoModel.allIndices({ month: 7 });   // 모기지수 내림차순
```

> **파이썬 백엔드가 필요한 경우**(REST API 등)에는 `mosquito_model_portable.py`를 그대로 `import`하면 됩니다.
> 웹 프런트와 파이썬 백엔드 어느 쪽을 쓰든 **같은 v3 수식·같은 결과**가 나오도록 두 파일을 동기화해 둡니다.

```python
from mosquito_model_portable import mosquito_index, all_indices, list_districts

# 실시간 날씨로 계산 (인터넷 필요, 실패 시 평년값 자동 대체)
r = mosquito_index('활천동', live_weather=True)
print(r['mosquito_index'])        # 예: 64.7
print(r['index_range'])           # {'low': ..., 'high': ...}
print(r['confidence']['level'])   # '높음' / '보통' / '낮음'

# 날씨 직접 입력
r = mosquito_index('생림면', temp_c=29, humidity=80, rain_3d_mm=50)

# 전 구역 순위 한 번에
ranked = all_indices(month=7)     # 모기지수 내림차순 리스트
```

반환 JSON에는 모기지수·등급·신뢰구간·발생잠재력·**인구노출**·발생원 분해·순위·행동요령이 모두 들어있습니다.

---

## 10. 랜덤포레스트 `.pkl`에 대한 정직한 노트

`build_model.py` / `retrain_with_population.py`가 만드는 `mosquito_risk_model.pkl`은
발생원+인구로 민원을 예측하는 **ML 모델**입니다. 재학습 결과, 솔직한 사실은:

- **가장 큰 개선은 "서부 결측 제외"** 였습니다 (LOO R² **−0.14 → +0.36**).
- **랜덤포레스트에선 인구가 거의 도움 안 됨** (0.356 → 0.335). 트리 모델이 이미 쓰는
  **공중화장실 개수가 인구와 중복**되기 때문입니다.
- **반면 배포용 수식 모델에선 인구가 크게 기여** (+0.58 → +0.81). 수식의 발생원 점수엔
  인구 proxy가 없었기 때문입니다.

> 결론: 구역이 17개뿐이라 ML(.pkl)은 실력을 못 냅니다(순위상관 0.67 < 수식 0.81).
> **실제 서비스가 끌고갈 것은 배포용 수식 모델(`mosquito_model_portable.py`)입니다.**
> `.pkl`은 "인구는 해롭지 않고, 결측 제외가 핵심"임을 확인한 **검증용 연구 자료**로 보세요.

---

## 11. 산출물 파일

**이 웹 저장소에 들어있는 것:**

| 파일 | 내용 |
|---|---|
| `mosquito-model.js` | **★실제 배포되는 웹앱 모델 (브라우저용, v3 동기화됨)** |
| `mosquito_model_portable.py` | 참조 구현 / 파이썬 백엔드용 (외부 의존 없음, 실시간 날씨 지원) |
| `모델_설명서.md` | 이 문서 |
| `인구데이터_필요성.md` | 인구·면적이 왜 필요한지 근거 정리 |

**분석 워크스페이스(연구·검증용, 웹 배포엔 불필요):** 발생원 집계 파이프라인,
인구 반영 재학습·교차검증 스크립트, `features_by_district.csv`,
RandomForest `.pkl`, 인구 반영 전/후 비교 리포트 등. (10번 참고)

---

## 12. 남은 한계와 다음 단계

- **면적(㎢) 미확보** → 지금은 "인구밀도"가 아니라 **"인구수(노출 규모)"** 로 반영.
  읍·면·동별 면적 17줄을 주시면 **발생원 밀도(발생원/면적)** 까지 넣어 농촌 착시를 한 번 더 교정 가능.
- **외국인 인구는 검토 후 제외(내국인 인구 사용)** — 총인구(내+외국인)로 바꾸면 서부·농촌 노출도가
  오르지만(한림·진례 외국인 비중 ~32%), 민원 상관이 +0.809→+0.792로 소폭 하락(외국인 민원 과소신고
  추정)하여 **내국인 기준 유지**. 필요 시 `2026년_6월말_김해시_외국인_등록_현황.xlsx`로 언제든 전환 가능.
- **화목동 인구 = 추정값(3,000명)** — 행정동 인구표 미수록(법정동). 정확값 있으면 교체.
- **서부 4개 구역** — 보건소 실측 자체가 없어 추정치. **현장 유충조사**가 근본 해결책.
- **구역 17개로 작다** → *예측*보다 *위험 스크리닝/우선순위*용으로 권장.
- **날씨 시차(lag)**: 모기는 기온·비에 ~10일 늦게 반응. 누적온도(GDD) 발육보정을 **파이썬·JS
  두 파일 모두에 도입 완료**. 다음 단계로 7·14일 강수 lag까지 넣으면 정밀도 추가 향상 여지.
</content>
