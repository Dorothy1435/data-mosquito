# 김해시 모기지수 모델 v4 — 설명서

작성일 2026-08-07 · 대상 파일 `service/mosquito_model_portable.py` (표준 라이브러리만 사용, 단일 파일)

---

## 0. 세 줄 요약

1. **면적 데이터를 확보해서 모델을 '개수'에서 '밀도'로 바꿨다.** v3는 면적이 없어 정화조 2,384개를 가진 한림면(59.7㎢)을 위험 1위로 뽑았는데, 실제 방역민원 밀도는 17개 구역 중 하위권이다.
2. **독립 검증 결과 v3는 부호가 반대였다.** 2025년 좌표 기반 방역민원 383건과의 스피어만 상관이 v3 **−0.258**, v4 **+0.951**.
3. **다만 새로 얻은 성충·유충 포집 이력의 99.6%가 합성 샘플 데이터**여서, 구역별 모기 밀도 학습에는 쓰지 않았다. 무엇을 썼고 무엇을 버렸는지는 5장에 전부 적었다.

---

## 1. 데이터 출처

김해시 방역현황보고 시스템(`sodamap.co.kr:8448`, 계정 gimhae)에서 수집했다. 수집·집계·검증 스크립트는 `sodamap_data/scripts/`에 전부 들어 있어 재실행하면 그대로 재현된다.

| 데이터 | 규모 | 모델 반영 | 비고 |
|---|---|---|---|
| 행정동 경계 폴리곤 + 주민등록 인구 | 19개 행정동 | ✅ **면적·인구밀도** | v3의 최대 한계였던 면적 확보 |
| 100m 인구격자 | 8,393셀 | ✅ 화목동 인구 분리 추정 | 법정동 단위 인구 배분 |
| 방역민원(좌표+일자+내용) | 383건 (2025년) | ✅ **검증 타겟** | v3는 구역 집계값만 보유 |
| 발생원 시설(좌표) | 18,810개소 / 16종 | ✅ 좌표로 구역 재배정 | 17,081개소 배정, 1,729개 좌표 결측 |
| 일별 관측기상 | 731일 (2024-08~2026-08) | ✅ **활동곡선 적합·월평년값** | 기온·습도·강수·풍속·7일누적 |
| 하천 수면 폴리곤 | 890개 | ✅ 수면비율 (출력용) | |
| 성충 포집 이력 | 114,050건 | ⚠️ **부분만** | 99.6%가 합성 샘플 — 5장 참조 |
| 유충 조사 이력 | 54,232건 | ❌ 미사용 | 구역별 검출률 93.9~96.5%로 신호 없음 |
| 헥사곤 우선지역 점수 | 1,211개 | ❌ 미사용 | 벤더 자체 모델 출력(참고 보관) |

수집 원본은 `sodamap_data/`에 저장했다.

---

## 2. 모델 구조

```
모기지수 = 100 × 기상활동지수 × (0.30 + 0.70 × 밀도위험)
                    ↑ 시간축(날씨)         ↑ 공간축(지역)
```

두 축을 곱하는 이유는 단순하다. 아무리 발생원이 많아도 1월엔 모기가 없고, 아무리 더워도 발생원과 사람이 없으면 물릴 일이 없다.

### 2-1. 공간축 — 밀도위험 (0~1)

```
밀도위험 = 0.5 × norm(log(발생원밀도)) + 0.5 × norm(log(인구밀도))

  발생원밀도 = Σ(시설유형별 개수 × 서식적합도 가중치) / 면적(㎢)
  인구밀도   = 주민등록인구 / 면적(㎢)
  norm(x)    = 17개 구역 log 스케일 min-max 정규화
```

**왜 밀도인가.** 모기지수는 "이 동네에 있으면 얼마나 물리나"에 답해야 한다. 그건 총량이 아니라 단위면적당 값이다. 상동면은 68.8㎢에 정화조가 흩어져 있고 인구는 2,872명이다. 회현동은 1.14㎢에 발생원 226개소와 7,981명이 몰려 있다. v3는 상동면을 11위·회현동을 13위로 뽑았지만, 실제 민원밀도는 상동면 0.38건/㎢, 회현동 27.98건/㎢다.

**왜 로그인가.** 발생원밀도가 42~533개/㎢로 10배 넘게 벌어지는데, 선형으로 쓰면 회현동 하나가 다른 16개 구역을 다 눌러버린다. 모기 밀도가 발생원에 선형 비례한다는 근거도 없다.

**가중치 0.5/0.5.** 13개 구역에 대해 0.05 간격 그리드 탐색을 했고, 0.4~0.6 구간이 모두 +0.95 부근으로 평평했다. 해석이 쉬운 0.5/0.5를 골랐다. 발생원밀도 단독은 +0.692, 인구밀도 단독은 +0.824로, 두 성분 모두 독립적으로 기여한다.

**시설 서식적합도 가중치** (`RISK_W`)

| 3.0 | 2.5 | 2.0 | 1.5 | 1.0 | 0.5 | 0.3 |
|---|---|---|---|---|---|---|
| 정화조, 개인하수처리 | 개인오수처리, 저수지, 폐타이어적치 | 축산농가, 타이어가게 | 폐기물처리, 고물상, 수경시설, 배수펌프장 | 공공하수처리, 도시공원 | 공중화장실, 목욕장 | 양봉농가 |

이 값은 문헌 기반 전문가 판단이며 데이터로 적합한 것이 아니다. 다만 정화조·오수처리시설이 배정 시설의 84%(14,396/17,081)라 가중치 변경에 대한 결과 민감도는 낮다.

### 2-2. 시간축 — 기상활동지수 (0~1)

```
활동지수 = 기온계수 × 습도계수 × 강수계수 × 풍속계수
```

**기온** — Brière 곡선. 731일 실측 기상에 적합했다.

```
f(T) = a·T·(T−T0)·(Tm−T)^(1/m) / 정규화상수
       a=0.000502, T0=10.5℃, Tm=40℃, m=1.164

유효기온 = 0.29 × 당일 평균기온 + 0.71 × 최근 7일 평균기온
```

7일 평균에 더 큰 가중치를 준 것이 v4의 핵심 개선 중 하나다. 모기는 오늘 날씨로 생기는 게 아니라 지난 일주일 동안 자란다. 유충 발육 지연을 이렇게 반영했다.

하한 T0=10.5℃는 데이터에서 적합된 값인데, *Culex* 발육영점 문헌값 10~11℃와 거의 정확히 일치한다. 상한 Tm은 한국 일평균기온이 33℃를 넘는 날이 없어 데이터로 제약되지 않으므로 문헌값 40℃로 고정했다.

| 기온 | 12℃ | 16℃ | 20℃ | 24℃ | 28℃ | **30℃** | 32℃ | 34℃ | 36℃ | 38℃ |
|---|---|---|---|---|---|---|---|---|---|---|
| **v4** | 0.07 | 0.32 | 0.59 | 0.83 | 0.98 | **1.00** | 0.97 | 0.88 | 0.71 | 0.45 |
| v3 | 0.01 | 0.12 | 0.42 | 0.85 | 0.98 | 0.85 | 0.64 | 0.42 | 0.24 | 0.12 |

v3는 27℃를 정점으로 잡고 34℃에서 0.42까지 떨어뜨렸다. 실데이터에서 채집량이 가장 많은 구간은 29~31℃였고, 34℃ 부근에서도 크게 줄지 않았다.

**습도** `0.648 + 0.020 × (H−40)/50`, 범위 0.30~1.05

기온을 통제하면 습도의 잔여 기여는 거의 0이었다(적합 시 기울기가 하한에 붙었다). v3는 습도에 0.6이라는 큰 가중치를 줬는데 근거가 없었다. 생리학적으로 습도가 성충 수명에 영향을 준다는 건 사실이므로 항 자체는 남겼지만, 실질 영향은 미미하게 줄였다.

**강수** `1 + 0.038·tanh(7일누적/40) − 0.034·tanh(당일강수/25)`, 범위 0.85~1.15

최근 강수는 산란처를 늘리고(+), 당일 폭우는 유충을 씻어낸다(−). 방향은 맞지만 크기는 작다. v3는 강수를 환경계수의 40%로 넣었는데 과대반영이었다.

**풍속** `1 − 0.096 × (풍속−1.5)/3`, 범위 0.55~1.15 — v3에 없던 항. 바람이 세면 모기가 날지 못한다.

### 2-3. 방제 우선순위 (당국용, 모기지수와 별개)

```
우선순위 = (밀도위험 × 인구)를 0~100으로 정규화 후 ^0.4 스케일
```

시민용 모기지수는 밀도지만, 방제 인력 배분은 총 피해 규모를 봐야 한다. 밀도위험이 낮아도 인구가 17만인 장유는 총 피해자 수가 크다. 민원 **건수**(밀도가 아니라)와의 스피어만이 v4 +0.765, v3 +0.479다.

`control_priority_ranking()` 으로 조회한다.

---

## 3. 검증 결과

독립 타겟은 2025년 방역민원 383건이다. 민원은 모델 입력에 전혀 쓰지 않았으므로 순환이 없다(단서는 5장).

### 3-1. 지역 위험

| | v4 | v3 |
|---|---|---|
| 민원밀도(건/㎢) ↔ 모기지수 | **+0.951** | −0.258 |
| 민원건수 ↔ 방제우선순위 | **+0.765** | +0.479 |
| LOO 안정성 (구역 1개씩 제외) | +0.937 ~ +0.958 | −0.371 ~ −0.161 |

### 3-2. 기상 활동곡선

| | v4 | v3 |
|---|---|---|
| 일별 채집수 731일 — R² | **0.709** | 0.500 |
| 일별 채집수 731일 — 스피어만 | **+0.906** | +0.879 |
| 실측 장비 102일 — 스피어만 | +0.381 | +0.402 |

**마지막 줄을 그냥 넘기지 말 것.** 실측 장비 데이터에서는 v3가 근소하게 높다. n=102이고 대부분 여름에 몰려 있어 기온 변동폭이 좁으므로 두 모델을 통계적으로 구분할 수 없다. v4의 기온곡선을 채택한 근거는 "실측에서 더 나았다"가 아니라 **문헌 정합성(발육영점 10.5℃)과 731일 전 계절 데이터에서의 설명력**이다.

### 3-3. 순위가 어떻게 바뀌었나 (7월 기준)

| 구역 | v3 | v4 | 실제 민원밀도 |
|---|---|---|---|
| 회현동 | 13위 | **1위** ↑ | 27.98 건/㎢ |
| 부원동 | 12위 | **2위** ↑ | 12.51 |
| 불암동 | 16위 | **5위** ↑ | 11.36 |
| 내외동 | 7위 | 4위 ↑ | 6.47 |
| 활천동 | 3위 | 6위 ↓ | 5.06 |
| 주촌면 | 2위 | 12위 ↓ | 1.02 |
| 생림면 | 10위 | 14위 ↓ | 0.24 |
| 상동면 | 11위 | **17위** ↓ | 0.38 |
| 한림면 | 4위 | 10위 ↓ | 결측 |

v3에서 상·하위였던 구역들이 거의 뒤집혔다. 민원밀도 순서와 비교하면 v4 쪽이 맞다.

### 3-4. 회귀 테스트

`sodamap_data/scripts/final_check.py` 가 매 실행 시 확인한다 — 17개 구역 × 12개월 전수 산출, JSON 직렬화, v3 시그니처(`rain_3d_mm`) 하위호환, 미등록 구역 예외처리. 현재 전부 PASS.

---

## 4. 유충 검출률을 왜 뺐나

v3는 보건소 유충 실태조사 검출률을 위험지수에 최대 60%까지 섞었다(`_breeding` 함수). 이건 역효과였다.

```
보건소 유충검출률 ↔ 민원밀도 : 스피어만 −0.715
```

| 구역 | 유충검출률 | 민원밀도 |
|---|---|---|
| 생림면 | 87% | 0.24 건/㎢ |
| 대동면 | 82% | 0.35 |
| 주촌면 | 78% | 1.02 |
| 내외동 | 27% | 6.47 |
| 부원동 | 28% | 12.51 |

이유는 명확하다. 유충 검출률은 **조사한 시설이 얼마나 지저분한가**를 재는 것이지 **그 동네에 모기가 얼마나 많은가**를 재는 게 아니다. 농촌 정화조는 관리가 덜 되니 검출률이 높지만, 주변에 물릴 사람이 없다.

v4는 이 값을 위험지수에서 완전히 빼고 `larva_survey` 필드로 별도 출력한다. 검출률 60% 이상이면 "시설 관리상태 불량 — 모기지수와 별개로 개선 대상"이라는 당국 권고가 뜬다. 버리는 게 아니라 **제 용도로 쓰는 것**이다.

---

## 5. 알려진 한계 — 반드시 읽을 것

**① 성충 포집 이력 114,050건 중 113,623건(99.6%)이 합성 샘플이다.**
`source_type = 'SAMPLE'`이고, 지점별 평균의 전반기↔후반기 스피어만이 **0.994**다. 즉 "지점마다 고정 배율 × 도시 공통 시계열 + 노이즈" 구조로 생성된 데이터다. 실제 장비 기록(MOSCOM/TEMP)은 427건·102일뿐이다.
→ **구역별 모기 밀도 학습에 쓰지 않았다.** 기온 반응곡선의 형태를 잡는 데만 썼고, 실측 427건으로 방향을 교차확인했다. 이 데이터로 구역 순위를 매겼다면 실제로는 균질한 값(31~52마리/트랩일)에 정밀도가 있는 척하는 결과가 나왔을 것이다.

**② sodamap 유충 이력 54,232건도 신호가 없다.** 구역별 검출률이 93.9~96.5% 범위로 균일하다. 미사용.

**③ 민원 표본이 작다.** 383건, 13개 구역. 진영읍·진례면·한림면·장유는 자료 미제공(결측)이라 검증에서 제외했다. 이 4개 구역의 모기지수는 인구·발생원 밀도로만 추정한 값이고, 모델이 `data_gap: true`와 낮은 신뢰도로 표기한다. **민원 0건을 안전으로 읽으면 안 된다.**

**④ 검증에 부분적 순환이 있다.** 민원은 사람이 신고하는 것이라 인구가 많은 곳에서 더 많이 발생하고, 인구밀도는 위험식에 들어가 있다. 다만 발생원밀도 단독으로도 민원밀도와 +0.692였으므로 순환만으로 설명되지는 않는다.

**⑤ 가중치가 13개 구역에서 정해졌다.** LOO에서 최소 +0.937로 안정적이었지만 표본이 작아 과적합 가능성은 남는다.

**⑥ 시설 좌표 결측 1,729개소(9.2%).** 정화조·오수처리시설에 몰려 있다. 해당 구역의 발생원밀도가 과소평가됐을 수 있다.

**⑦ 화목동은 추정 구역이다.** 공식 행정동이 아닌 법정동이라(칠산서부동 소속) 100m 인구격자로 인구를 분리 추정했다. 신뢰도가 한 단계 낮게 표기된다.

**⑧ 기온 상한 Tm=40℃는 데이터가 아니라 문헌값이다.** 한국 일평균기온이 33℃를 안 넘어서 실측으로 제약할 수 없다. 폭염 상황의 예측은 외삽이다.

---

## 6. 사용법

```python
from mosquito_model_portable import mosquito_index, all_indices, list_districts, control_priority_ranking

# 실시간 기상 자동 조회 (Open-Meteo, 키 불필요)
r = mosquito_index('회현동', live_weather=True)

# 직접 입력
r = mosquito_index('회현동', temp_c=29, humidity=80, rain_7d_mm=50,
                   ta_7d_avg=28, wind_ms=1.6)

# 기상 미입력 → 실측 월평년값 사용
r = mosquito_index('회현동', month=7)

# 전 구역
rows = all_indices(month=7)                 # 모기지수 내림차순
rows = all_indices(live_weather=True)       # 17개 구역 날씨를 1회 호출로

# 방제 우선순위
for x in control_priority_ranking():
    print(x['rank'], x['district'], x['control_priority'])
```

`rain_3d_mm` 인자는 v3 호환용으로 남아 있고 `rain_7d_mm`으로 처리된다. `service/api_server.py`, `service/main.py`는 수정 없이 그대로 동작한다.

### 반환 예시 (회현동, 29℃/80%/7일강수 50mm)

```json
{
  "district": "회현동",
  "mosquito_index": 64.8,
  "index_range": { "low": 58.3, "high": 71.3 },
  "level": 3, "grade": "주의", "color": "#f59e0b",
  "confidence": { "level": "높음", "uncertainty_pct": 10.0,
                  "reasons": { "weather": "직접입력", "population": "주민등록",
                               "complaint_data": "보유" } },
  "weather": {
    "effective_temp_c": 28.3, "activity_index": 0.672,
    "components": {
      "temperature": { "score": 0.984, "status": "최적 — 발생 정점 구간" },
      "humidity":    { "score": 0.664, "status": "다습 — 성충 생존에 유리" },
      "rainfall":    { "score": 1.032, "status": "최근 강수 많음 — 산란처 확대" },
      "wind":        { "score": 0.997, "status": "약풍 — 활동에 지장 없음" }
    }
  },
  "area": { "area_km2": 1.14, "population": 7981, "pop_density": 6979.0,
            "water_pct": 0.377, "park_m2_per_km2": 102946 },
  "source_risk": {
    "density_risk": 0.949, "breed_density_per_km2": 533.4,
    "control_priority": 41.7, "control_priority_rank": 6,
    "total_facilities": 226,
    "top_sources": [
      { "source": "정화조(청소대상)", "count": 62, "per_km2": 54.4, "risk_contribution_pct": 23.3 },
      { "source": "개인하수처리시설", "count": 123, "per_km2": 107.9, "risk_contribution_pct": 23.3 }
    ]
  },
  "larva_survey": { "surveyed": 23, "positive": 10, "detection_rate": 0.435,
                    "used_in_index": false,
                    "note": "발생원 시설의 관리상태 지표입니다. 민원밀도와 음의 상관(-0.715)이어서 모기지수 산출에는 쓰지 않고…" },
  "ranking": { "rank": 1, "total_districts": 17, "city_avg_index": 43.0 },
  "advice": { "citizen": [...], "authority": [...] }
}
```

### 등급 구간

| 지수 | 단계 | 등급 | 색상 |
|---|---|---|---|
| 0–25 | 1 | 쾌적 | `#3b82f6` |
| 25–50 | 2 | 관심 | `#22c55e` |
| 50–75 | 3 | 주의 | `#f59e0b` |
| 75–100 | 4 | 불쾌 | `#ef4444` |

### 신뢰구간

`uncertainty = √(기상불확실성² + 민원결측² + 인구추정²)`
기상 실측/입력 0.06 · 월평년값 0.20 / 민원 보유 0.08 · 결측 0.22 / 인구 격자추정 0.10

---

## 7. 재현 방법

```
sodamap_data/scripts/
  soda.py wfs.py geo.py         공통 유틸 (로그인 · WFS · 지오연산)
  harvest1~3.py                 데이터 수집
  aggregate2.py                 17구역 피처 집계 → district_features.json
  fit_weather.py                731일 기상 적합 → weather_fit.json
  validate2.py                  위험식 그리드 탐색 + LOO
  gen_model_data.py             모델 상수 블록 생성
  build_v4.py                   mosquito_model_portable.py 조립
  final_check.py                v3 대비 검증 + 회귀 테스트
```

`python harvest1.py && python harvest2.py && python harvest3.py && python aggregate2.py && python fit_weather.py && python gen_model_data.py && python build_v4.py && python final_check.py`

`soda.py`에 로그인 계정이 평문으로 들어 있다. 저장소를 외부에 공유할 계획이면 환경변수로 빼는 편이 좋다.

v3 원본은 `sodamap_data/mosquito_model_v3_backup.py`에 보관돼 있다.

---

## 8. 다음에 하면 좋을 것

1. **실측 장비(MOSCOM) 데이터 축적** — 현재 427건뿐이다. 한 시즌만 더 쌓이면 구역별 모기 밀도를 합성 데이터 없이 직접 학습할 수 있고, 그때는 밀도위험 가중치를 민원이 아닌 실제 채집량으로 적합할 수 있다.
2. **민원 결측 4개 구역 자료 확보** — 진영읍·진례면·한림면·장유. 특히 장유는 인구 17.8만으로 방제 우선순위 1위인데 검증이 안 된다.
3. **시설 좌표 결측 1,729개소 보정** — 주소 지오코딩으로 채우면 발생원밀도 정확도가 올라간다.
4. **읍면동보다 작은 단위** — 헥사곤(1,211개) 또는 100m 격자로 내려가면 같은 동 안에서도 위험 구역을 짚을 수 있다. 데이터는 이미 다 있다.
5. **모기 종 구분** — 현재는 종 구분 없는 총량 모델이다. 일본뇌염매개(작은빨간집모기)와 흰줄숲모기는 서식지·활동시간이 달라 방제 방법도 다르다.
