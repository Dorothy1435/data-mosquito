# 김해시 모기 발생 위험도 모델 — 설명서

## 한 줄 요약
> 김해시의 **시설·민원·유충조사 데이터**를 읍·면·동별로 합쳐서,
> **"어디가 모기 위험이 높고, 어디부터 방제해야 하는지"**를 점수로 알려주는 모델입니다.
> 여기에 **날씨(실시간)**를 곱해 "오늘 이 동네 모기지수는 몇 점"까지 계산합니다.

---

## 1. 왜 만들었나 — 핵심 아이디어

모기는 **고인 물 + 유기물**이 있는 곳에서 번식합니다.
그러니 그런 **발생원 시설**(정화조·축사·저수지·폐타이어 등)이
어디에 몰려 있는지 세면 위험도를 점수화할 수 있습니다.

여기에 **실제 방역 민원**과 **유충 조사 결과**를 결합하면,
단순 추정이 아니라 *실제로 모기가 나오는 곳*을 가려낼 수 있습니다.

```
발생원 시설 (어디에 번식처가 많은가)
        +
실제 민원 (어디서 사람들이 불편을 겪는가)
        +
유충 검출 (실제로 모기 애벌레가 나왔는가)   ← v2에서 추가
        +
날씨 (오늘 모기가 활동하기 좋은가)         ← 실시간 연동
        ↓
   구역별 모기지수 · 방제 우선순위
```

---

## 2. 두 개의 산출물

이 프로젝트는 결과가 **두 갈래**입니다. 헷갈리지 않게 구분하세요.

| | **방제 우선순위** (정적) | **모기지수** (동적) |
|---|---|---|
| 무엇 | 구역별 *연간* 방제 우선순위 | *오늘* 그 동네 모기 활동 점수 |
| 만든 곳 | `build_model.py` | `service/mosquito_model_portable.py` |
| 입력 | 시설·민원·유충 데이터 | 위 결과 + 날씨 |
| 용도 | 예산·인력 배분 (행정) | 시민용 일일 예보 (앱/웹) |
| 변하나? | 데이터 갱신 시에만 | 날씨 따라 매일 |

---

## 3. 사용한 데이터

| 데이터 | 역할 | 매칭 건수 |
|---|---|---|
| 개인하수처리시설(정화조·오수) | 발생원 (최대 유충서식처) | 9,579 |
| 양돈·한우 농가 | 발생원 (분뇨·물웅덩이) | 761 |
| 저수지 현황 | 발생원 (정체수) | 110 |
| 타이어가게 | 발생원 (폐타이어 산란처) | 99 |
| 폐기물 재활용/처리업체 | 발생원 (고인물·용기) | 769 |
| 수경시설(녹지과·물놀이형) | 발생원 (정체수) | 33 |
| 공중화장실 | 발생원 + 도심밀집 지표 | 563 |
| 포충기 설치 현황 | 감시 인프라 (참고) | 12 |
| **방역소독 민원대장(2025)** | **검증 타겟 (실제 민원)** | 196 |
| **유충 실태조사** | **위험지수 실측 보정 (v2)** | 1,000+ |

> `.hwp/.pdf` 문서(하천·하수처리·배수펌프장·인명피해)는 자동 파싱이 불안정해 1차 제외.
> 표(엑셀/CSV)로 주시면 발생원에 추가할 수 있습니다.

---

## 4. 계산 과정

1. **주소 → 행정구역 추출** — 모든 데이터의 주소에서 읍·면·동 자동 추출 (정화조 기준 매칭률 98%)
2. **구역별 집계** — 시설 유형별 개수를 17개 구역에 집계 → `features_by_district.csv`
3. **위험지수(0~100)** — 각 발생원을 0~1로 정규화 후 곤충학적 가중치로 합산
   - 정화조 3.0 · 저수지 2.5 · 축산 2.0 · 타이어 2.0 · 폐기물재활용 1.5 · 수경시설 1.5 · 화장실 0.5
4. **검증** — 발생원 → 민원 회귀모델(RandomForest + 포아송)로 어떤 발생원이 실제 민원과 연결되는지 확인
5. **방제 우선순위(0~100)** — 발생원위험 0.55 + 실제 민원압력 0.45 결합

### 모기지수(동적)는 여기에 날씨를 곱합니다
```
모기지수 = 100 × 날씨활동지수 × (0.35 + 0.65 × 발생원위험)
```
- **날씨활동지수**: 기온(27°C 최적)·습도(높을수록↑)·강수(적당한 비↑, 폭우↓)를 0~1로
- `0.35`는 날씨가 좋으면 발생원이 적어도 최소한의 모기는 있다는 기본값

---

## 5. 핵심 결과 (방제 우선순위 상위)

| 순위 | 구역 | 발생원위험 | 민원 | **방제우선순위** |
|---|---|---|---|---|
| 1 | 활천동 | 38.5 | 37 | **100.0** |
| 2 | 한림면 | 100.0 | 0 | 83.1 |
| 3 | 상동면 | 43.6 | 21 | 74.8 |
| 4 | 주촌면 | 37.7 | 23 | 73.6 |
| 5 | 북부동 | 32.6 | 23 | 69.4 |

전체는 `risk_ranking.csv` 참고.

---

## 6. 모델이 알려준 중요한 사실

- **발생원 개수만으로는 민원을 못 맞힌다** (위험지수 vs 민원 상관 −0.14).
  → **인구 교란**: 농촌 면지역(한림·생림 등)은 가구별 개별정화조로 발생원은 많지만 인구가 적어 민원이 거의 없고,
  도심 동지역(활천·북부)은 발생원이 적어도 인구가 많아 민원이 몰린다.
- 회귀 해석상 **정화조(+0.49)·수경시설(+0.44)** 이 도심 민원과 강하게 연결.
- 그래서 단순 위험지수보다 **발생원 + 실제 민원을 결합한 방제 우선순위**가 실무적으로 타당.

---

## 7. v2 보완 내용 (`mosquito_model_portable.py`)

기존 모델의 약점을 3가지 보완했습니다.

### A. 위험지수 실제화 — 유충 검출률 반영
발생원 *개수*뿐 아니라 **실제 유충이 검출된 비율**을 위험지수에 직접 섞었습니다.
- 표본이 많을수록 실측 비중↑ (최대 60%)
- 예) 생림면은 158건 조사 중 138건 양성(**87%**) → 발생원 점수 0.53 → **0.74로 상향**
- 조사 안 한 구역(한림·진영·진례·장유)은 발생원 추정만 사용 + **신뢰도 낮음**으로 표시

### C. 실시간 기상 연동 — Open-Meteo (구역별 날씨)
`live_weather=True`로 호출하면 **현재 날씨를 자동으로 받아와** 계산합니다.
- API 키 불필요, 표준 라이브러리(urllib)만 사용 → 파일 하나로 이식 가능 유지
- **구역별 좌표(`COORDS`)** 를 채워, 17개 읍·면·동이 **각자 다른 날씨**로 계산됨
  (Open-Meteo 격자 ~1~11km. 예: 같은 시각 장유 20.8°C / 한림면 22.0°C로 갈림)
- `all_indices(live_weather=True)`는 **멀티좌표 1회 호출**로 17곳을 한 번에 받아옴 (API 17회 호출 방지)
- 인터넷 실패 시 **월평년값으로 자동 대체** (서비스 중단 없음)
- ※ `COORDS`는 읍·면·동 중심부 *근사* 좌표. 더 정밀하게 하려면 통계청 행정동 경계의 공식 중심좌표로 교체

### D. 불확실성 출력 — 신뢰구간 + 신뢰도 등급
단일 점수의 *거짓 정밀도*를 없애고 **범위와 신뢰도**를 함께 반환합니다.
- 날씨가 실측/직접입력이고 유충 표본이 많으면 → **신뢰도 높음**, 좁은 구간
- 평년값 + 유충 조사 없음이면 → **신뢰도 낮음**, 넓은 구간

```python
from mosquito_model_portable import mosquito_index

# 실시간 날씨로 계산
r = mosquito_index('활천동', live_weather=True)
print(r['mosquito_index'])      # 40.4
print(r['index_range'])         # {'low': 37.0, 'high': 43.8}
print(r['confidence']['level']) # '높음'

# 날씨 직접 입력
r = mosquito_index('생림면', temp_c=29, humidity=80, rain_3d_mm=50)
# 87% 유충 검출 반영 → effective_geo 0.74, 신뢰도 높음
```

---

## 8. 산출물 파일

| 파일 | 내용 |
|---|---|
| `service/mosquito_model_portable.py` | **배포용 모델** (파일 1개, 외부 의존 없음) |
| `build_model.py` | 전체 학습 파이프라인 (재현·확장) |
| `features_by_district.csv` | 구역별 발생원 집계 원본 |
| `risk_ranking.csv` | 구역별 위험지수·민원·방제우선순위 (최종 순위표) |
| `mosquito_risk_model.pkl` | 학습된 RandomForest 모델 |
| `model_report.txt` | 전체 수치 리포트 |

---

## 9. 남은 한계와 다음 단계

- **구역 17개로 작다** → *예측*보다 *위험 스크리닝/우선순위*용으로 권장.
- **인구·면적 추가** 시 "단위인구당 발생원 밀도"로 교란 제거 → 예측력 향상 (가장 효과 큼).
- **날씨 시차(lag)**: 모기는 기온·비에 ~10일 늦게 반응. 누적온도(GDD)·7·14일 강수 도입 시 정밀도↑.
- **포충기 채집 마리수(시계열)** 확보 시 진짜 개체수 타겟으로 정밀 모델 가능.
- 폐기물업체는 위경도가 있어 **격자 히트맵**으로 확장 가능.
