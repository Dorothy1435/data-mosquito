# 김해시 모기 발생 위험도 모델 — 설명서

## 1. 무엇을 만들었나
김해시의 여러 행정 데이터(시설 현황 + 방역 민원 + 유충조사)를 **읍·면·동 단위로 통합**해
구역별 **모기 발생 위험도**와 **방제 우선순위**를 산출하는 모델을 만들었습니다.

핵심 아이디어: 모기는 *고인 물 + 유기물*이 있는 곳에서 번식한다 → 그런 **발생원 시설**이
어디에 얼마나 몰려 있는지 집계하면 위험도를 점수화할 수 있다. 여기에 **실제 방역 민원**을
결합해 "어디부터 방제해야 하는가"를 정한다.

## 2. 사용한 데이터와 역할
| 데이터 | 모델에서의 역할 | 매칭 건수 |
|---|---|---|
| 개인하수처리시설(정화조·오수처리) | 발생원(최대 유충서식처) | 9,579 |
| 양돈·한우 농가 | 발생원(분뇨·물웅덩이) | 761 |
| 저수지 현황 | 발생원(정체수) | 110 |
| 타이어가게 | 발생원(폐타이어 산란처) | 99 |
| 폐기물 재활용/처리업체 | 발생원(고인물·용기) | 769 |
| 수경시설(녹지과·물놀이형) | 발생원(정체수) | 33 |
| 공중화장실 | 발생원 + 도심밀집 지표 | 563 |
| 포충기 설치 현황 | 감시 인프라(참고) | 12 |
| **방역소독 민원대장(2025)** | **검증 타겟(실제 모기 민원)** | 196 |
| 유충 실태조사 | 보조 검증(유충 검출 여부) | — |

> 참고: 하천현황·공공하수처리·배수펌프장은 `.hwp/.hwpx` 한글문서, 인명피해는 `.pdf`라
> 자동 파싱이 불안정해 이번 1차 모델에서는 제외했습니다. 표 형태로 변환해 주시면 발생원에 추가 가능합니다.

## 3. 처리 과정
1. **주소 → 행정구역 추출기**: 모든 데이터의 주소에서 읍·면·동을 자동 추출
   (도로명주소·지번주소·OCR 약식표기 정규화 포함). 정화조 기준 매칭률 98%.
2. **구역별 집계**: 시설 유형별 개수를 구역(17개)별로 집계 → `features_by_district.csv`
3. **위험지수(risk_index)**: 각 발생원을 0~1 정규화 후 곤충학적 가중치로 합산(0~100)
   - 정화조 3.0 · 저수지 2.5 · 축산 2.0 · 타이어 2.0 · 폐기물재활용 1.5 · 수경시설 1.5 · 화장실 0.5
4. **검증 회귀모델**: 발생원 시설 → 방역민원 건수 (RandomForest + 포아송회귀, LOO 교차검증)
5. **방제 우선순위(control_priority)**: 발생원위험 0.55 + 실제민원압력 0.45 결합(0~100)

## 4. 핵심 결과 (방제 우선순위 상위)
| 순위 | 구역 | 발생원위험 | 민원건수 | **방제우선순위** |
|---|---|---|---|---|
| 1 | 활천동 | 38.5 | 37 | **100.0** |
| 2 | 한림면 | 100.0 | 0 | 83.1 |
| 3 | 상동면 | 43.6 | 21 | 74.8 |
| 4 | 주촌면 | 37.7 | 23 | 73.6 |
| 5 | 북부동 | 32.6 | 23 | 69.4 |
| 6 | 생림면 | 43.6 | 9 | 52.8 |
| 7 | 대동면 | 20.8 | 18 | 50.4 |

전체 순위는 `risk_ranking.csv` 참고.

## 5. 모델이 알려준 중요한 사실
- **발생원 개수만으로는 민원을 예측하기 어렵다** (LOO R² ≈ 음수, 발생원개수 vs 민원 상관 −0.14).
  이유는 **인구 교란효과**: 농촌 면지역(한림·상동·생림 등)은 가구별 개별정화조 때문에
  발생원 수는 압도적이지만 인구가 적어 민원이 거의 없고, 도심 동지역(활천·부원·북부)은
  발생원이 적어도 인구가 많아 민원이 집중된다.
- 포아송 회귀 해석: **정화조/오수(+0.49)**, **수경시설(+0.44)** 이 민원과 양(+)의 관계.
  → 도심에서 모기 민원을 키우는 핵심 발생원은 **정화조·수경시설**.
- 따라서 단순 위험지수가 아니라 **발생원 + 실제 민원을 결합한 방제 우선순위**가
  실무적으로 가장 타당한 지표.

## 6. 산출물 파일
- `mosquito_risk_model.pkl` — 학습된 RandomForest 모델 + 피처/가중치
- `features_by_district.csv` — 구역별 발생원 집계 원본 피처 테이블
- `risk_ranking.csv` — 구역별 위험지수·민원·방제우선순위 (최종 순위표)
- `model_report.txt` — 전체 수치 리포트
- `build_model.py` — 전체 파이프라인 (재현·확장 가능)

## 7. 한계와 개선 방향
- 구역 단위가 17개로 작아 *예측형* 모델보다 *위험 스크리닝/우선순위* 모델로 활용 권장.
- **인구·면적 데이터**를 추가하면 "단위인구당 발생원 밀도"로 교란을 제거해 예측력 향상.
- **포충기 채집 마리수**(시계열)가 있으면 진짜 모기 개체수 타겟으로 정밀 모델 가능.
- 하천·하수처리·배수펌프장(.hwp) 표를 추가하면 발생원 커버리지 확대.
- 폐기물업체는 위경도 좌표가 있어, 좌표 기반 **격자 히트맵**으로 확장 가능.
